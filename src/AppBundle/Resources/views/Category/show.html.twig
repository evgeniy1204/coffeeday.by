{% extends 'AppBundle::base.html.twig' %}
{% block content %}
<section class="content-header">
  <h1>
    Категории продуктов
  </h1>
</section>
<section class="content">
    <div class="row">
        <div class="col-md-12">
            <div class="box">
                <div class="box-body no-padding">
                  <table class="table">
                    <tr>
                        <th>ID</th>
                        <th>Название категории</th>
                        <th>Действия</th>
                    </tr>
                    <tr>
                        <td><a href="{{ path('product_show', { 'id': category.id }) }}">{{ category.id }}</a></td>
                        <td><a href="{{ path('product_show', { 'id': category.id }) }}">{{ category.title }}</a></td>
                        <td>
                            <a href="{{ path('category_edit', { 'id': category.id }) }}"><button class="btn bg-purple btn-flat margin">Редактировать</button></a>
                            {{ form_start(delete_form) }}
                                <button type="submit" class="btn bg-purple btn-flat margin">Удалить</button>
                            {{ form_end(delete_form) }}
                        </td>
                    </tr>
                  </table>
                </div><!-- /.box-body -->
            </div><!-- /.box -->
        </div>
    </div>
</section>
{% endblock %}
